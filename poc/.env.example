# Baremetal Automation — Environment Configuration Template
#
# The actual .env lives at ../config/.env  (one level above poc/, never committed)
# Run `make setup` to create it from this template.
#
# PKI certs live at ../pki/  (also never committed)
# Run `bash scripts/pki/setup_pki.sh` to generate them.

# NetBox Configuration
NETBOX_URL=http://netbox.example.com
NETBOX_TOKEN=your-netbox-api-token-here
NETBOX_TENANT=baremetal-staging

# iLO / BMC Configuration
ILO_DEFAULT_USER=Administrator
ILO_DEFAULT_PASSWORD=your-ilo-password-here

# Callback API URL (must be reachable from PXE-booted servers)
CALLBACK_API_URL=https://10.x.x.x:5000

# Monitoring Interval (seconds)
MONITORING_INTERVAL_SECONDS=300

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=your-redis-password-here

# Redis TLS (mTLS — workers authenticate to Redis with client cert)
REDIS_USE_TLS=true
REDIS_TLS_CERT=/path/to/pki/workers/worker-cert.pem
REDIS_TLS_KEY=/path/to/pki/workers/worker-key.pem
REDIS_TLS_CA=/path/to/pki/ca/ca-cert.pem

# Callback API TLS
API_USE_TLS=true
API_TLS_CERT=/path/to/pki/api/api-cert.pem
API_TLS_KEY=/path/to/pki/api/api-key.pem
API_TLS_CA=/path/to/pki/ca/ca-cert.pem
API_REQUIRE_CLIENT_CERT=false

# Anthropic API (for chatbot portal)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Vapi voice integration
VAPI_PUBLIC_KEY=your-vapi-public-key-here
VAPI_SERVER_URL=https://your-public-host-or-ngrok-url
